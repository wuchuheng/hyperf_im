import React from "react";
import styles from './index.less';

const LeftBar = (props: any) => {
  return (
    <ul className={styles.leftBarWrapper}>
      <li>默认接入网站</li>
    </ul>
  );
};

export default LeftBar;
